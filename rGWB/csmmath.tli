
#ifndef csmmath_hxx
#define csmmath_hxx

enum csmmath_classify_point_t
{
    CSMMATH_CONTAIMENT_POINT_BELOW,
    CSMMATH_CONTAIMENT_POINT_ON,
    CSMMATH_CONTAIMENT_POINT_ABOVE
};

enum csmmath_containment_point_loop_t
{
    CSMMATH_CONTAINMENT_POINT_LOOP_INTERIOR,
    CSMMATH_CONTAINMENT_POINT_LOOP_ON_VERTEX,
    CSMMATH_CONTAINMENT_POINT_LOOP_ON_HEDGE
};

enum csmmath_dropped_coord_t
{
    CSMMATH_DROPPED_COORD_X,
    CSMMATH_DROPPED_COORD_Y,
    CSMMATH_DROPPED_COORD_Z
};

#define CSMMATH_MIN(A, B) (((A) < (B)) ? (A): (B))
#define CSMMATH_MAX(A, B) (((A) > (B)) ? (A): (B))

#define CSMMATH_CUAD(X) ((X) * (X))

#define CSMMATH_PI 3.14159265359

#define CSMMATH_ABS(x) (((x) >= 0.) ? (x) : -(x))

#endif
